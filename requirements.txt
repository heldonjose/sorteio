# Framework
Django==5.2

# Configuração
python-dotenv
dj-database-url

# Banco
psycopg[binary]

# HTTP (cliente da API do Instagram)
requests

# Celery
celery[redis]
django-celery-beat
django-celery-results

# Front (binário standalone, sem Node)
django-tailwind-cli

# Segurança / criptografia de tokens
cryptography

# Servidor (produção)
gunicorn[gevent]

# Deploy remoto
fabric

# Linter
ruff

# Testes
pytest
pytest-django
pytest-playwright
factory-boy

# Monitoramento de erros
sentry-sdk[django]
